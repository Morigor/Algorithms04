public class IsPositiveNumber {

    //  TASK 5  Напишите алгоритм IsPositiveNumber, который возвращает true,
    //          если численный параметр больше или равен нулю, и возвращает false,
    //          если параметр меньше 0.

    public boolean isPositiveNumberAlgo(int number) {
            return number >= 0;
    }
}
